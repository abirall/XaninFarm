[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py tests.py
testpaths = apps
addopts =
    --strict-markers
    --reuse-db
    -ra
markers =
    slow: tests that take noticeably longer (concurrency simulations)
filterwarnings =
    ignore::DeprecationWarning:kombu.*
